# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Node (only if tooling ever pulls deps)
node_modules/

# Claude Code session data
.claude/

# Secrets — never commit service-role keys. (Supabase anon key is public-safe.)
.env
.env.*
*.local
